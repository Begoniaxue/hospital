<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.HealthAttachmentMapper">

    <select id="selectByPatientId" resultType="com.hospital.entity.HealthAttachment">
        SELECT * FROM health_attachment
        WHERE patient_id = #{patientId} AND deleted = 0
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectListByPatientId" resultType="com.hospital.entity.HealthAttachment">
        SELECT * FROM health_attachment
        WHERE patient_id = #{patientId} AND deleted = 0
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
