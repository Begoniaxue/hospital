<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.InpatientRecordMapper">

    <select id="selectRecordPage" resultType="com.hospital.entity.InpatientRecord">
        SELECT * FROM inpatient_record
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (patient_name LIKE CONCAT('%', #{keyword}, '%') OR inpatient_no LIKE CONCAT('%', #{keyword}, '%') OR title LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="inpatientId != null">
            AND inpatient_id = #{inpatientId}
        </if>
        ORDER BY record_date DESC, create_time DESC
    </select>

</mapper>
