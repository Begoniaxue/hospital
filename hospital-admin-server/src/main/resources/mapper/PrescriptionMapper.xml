<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.PrescriptionMapper">

    <select id="selectPrescriptionPage" resultType="com.hospital.entity.Prescription">
        SELECT * FROM prescription
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                prescription_no LIKE CONCAT('%', #{keyword}, '%')
                OR patient_name LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="startTime != null and startTime != ''">
            AND create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>
