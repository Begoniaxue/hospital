<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.BedMapper">

    <select id="selectBedPage" resultType="com.hospital.entity.Bed">
        SELECT * FROM bed
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (bed_no LIKE CONCAT('%', #{keyword}, '%') OR ward_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="wardId != null">
            AND ward_id = #{wardId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY bed_no ASC
    </select>

</mapper>
