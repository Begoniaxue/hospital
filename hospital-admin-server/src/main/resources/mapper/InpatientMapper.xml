<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.InpatientMapper">

    <select id="selectInpatientPage" resultType="com.hospital.entity.Inpatient">
        SELECT * FROM inpatient
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                inpatient_no LIKE CONCAT('%', #{keyword}, '%')
                OR patient_name LIKE CONCAT('%', #{keyword}, '%')
                OR diagnosis LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
