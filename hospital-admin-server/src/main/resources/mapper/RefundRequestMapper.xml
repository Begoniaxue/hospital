<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.RefundRequestMapper">

    <select id="selectRefundPage" resultType="com.hospital.entity.RefundRequest">
        SELECT * FROM refund_request
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (
                refund_no LIKE CONCAT('%', #{keyword}, '%')
                OR patient_name LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="startTime != null and startTime != ''">
            AND apply_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND apply_time &lt;= #{endTime}
        </if>
        ORDER BY create_time DESC
    </select>
</mapper>
