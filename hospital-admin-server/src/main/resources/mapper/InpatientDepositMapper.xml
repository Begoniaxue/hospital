<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.InpatientDepositMapper">

    <select id="selectDepositPage" resultType="com.hospital.entity.InpatientDeposit">
        SELECT * FROM inpatient_deposit
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (patient_name LIKE CONCAT('%', #{keyword}, '%') OR inpatient_no LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="inpatientId != null">
            AND inpatient_id = #{inpatientId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>
